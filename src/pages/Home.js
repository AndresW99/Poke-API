import React from 'react';
import { Saludo } from '../components/Saludo';

// Renderizamos el componente saludo en la pagina home
export const Home = () => {

    return (
        <div>
            <Saludo />
        </div>
    )
}

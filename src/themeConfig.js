import { createTheme } from '@material-ui/core/styles';

// Config global de Material-UI para cambiar el color de la tabla
export const theme = createTheme({
    palette: {
        background: {
            paper: '#F4F2B7'
        }
    }
})